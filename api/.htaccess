Options -Indexes
<FilesMatch "^(podcast|emergency-poll)\.php$">
  Require all granted
</FilesMatch>
<FilesMatch "^(?!(podcast|emergency-poll)\.php$).+">
  Require all denied
</FilesMatch>
<IfModule mod_headers.c>
  Header always set Cache-Control "no-store, private"
  Header always set X-Content-Type-Options "nosniff"
</IfModule>
